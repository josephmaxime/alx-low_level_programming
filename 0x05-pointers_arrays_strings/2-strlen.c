#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "main.h"

/**
 * _strlen - change value of poiter to 98
 *
 * @s: pointer
 * Return: int value of length
 */

int _strlen(char *s)
{
	int length;

	length = strlen(s);

	return (length);
}
